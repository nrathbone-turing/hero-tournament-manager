// File: frontend/src/components/EventDetail.jsx
// Purpose: Displays details for a single Event, including entrants and matches.
// Notes:
// - Fetches data from backend `/events/:id`, `/entrants?event_id=:id`, and `/matches?event_id=:id`.
// - Currently a placeholder to satisfy imports and allow tests to run.

import React from "react"

export default function EventDetail() {
  return (
    <div>
      <h1>Event Detail</h1>
      <p>Placeholder content</p>
    </div>
  )
}
